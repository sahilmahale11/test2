import { styled, Box } from '@mui/material';
export const StyledBox = styled(Box)({
    height: 350,
    marginLeft: 'auto',
    marginRight: 'auto',
    backgroundColor: '#F6F5F8',
    cursor: 'pointer',
    marginTop: '19px',
});
